# Инструкция по запуску проекта для клиента

## 🚀 Быстрый старт

### Шаг 1: Клонирование репозитория с submodule

**Вариант 1 (рекомендуется) - клонирование с автоматической инициализацией submodule:**

```bash
git clone --recurse-submodules https://github.com/global50/ms-yurservice.git
cd ms-yurservice
```

**Вариант 2 - если уже склонировали без флага `--recurse-submodules`:**

```bash
git clone https://github.com/global50/ms-yurservice.git
cd ms-yurservice
git submodule update --init --recursive
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

Зависимости установятся автоматически для основного проекта и микрофронтенда через npm workspaces.

### Шаг 3: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### Шаг 4: Запуск проекта

```bash
npm run dev
```

Откройте в браузере:
- `http://localhost:5173` - основное приложение
- `http://localhost:5173/yurservice` - страница микросервиса YurService

## 📋 Полный список команд

```bash
# 1. Клонирование с submodule
git clone --recurse-submodules https://github.com/global50/ms-yurservice.git

# 2. Переход в директорию проекта
cd ms-yurservice

# 3. Установка зависимостей
npm install

# 4. Создание .env файла (скопируйте из примера выше и заполните своими данными)
# Создайте файл .env вручную или используйте:
cat > .env << EOF
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
EOF

# 5. Запуск проекта
npm run dev
```

## 🔄 Обновление submodule

Если нужно обновить микрофронтенд до последней версии:

```bash
# Обновить submodule до последней версии из main ветки
git submodule update --remote microfrontend-yurservice

# Или вручную
cd microfrontend-yurservice
git pull origin main
cd ..
```

## ❓ Частые вопросы

### Что делать, если submodule пустой после клонирования?

Если вы склонировали репозиторий без флага `--recurse-submodules`, выполните:

```bash
git submodule update --init --recursive
```

### Как проверить, что submodule правильно подключен?

```bash
# Проверьте статус submodule
git submodule status

# Должно показать:
# c7521a52b23cb4c754601fbd25ba724f96d41fc9 microfrontend-yurservice (heads/main)
```

### Что делать, если возникли ошибки при установке зависимостей?

```bash
# Очистите и переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

## 📚 Дополнительная информация

- [README.md](./README.md) - общая информация о проекте
- [START.md](./START.md) - подробная инструкция по запуску
- [SUBMODULE_TROUBLESHOOTING.md](./SUBMODULE_TROUBLESHOOTING.md) - решение проблем с submodule

