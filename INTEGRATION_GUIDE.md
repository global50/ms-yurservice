# Руководство по интеграции YurService Microfrontend

## Краткое описание

Микросервис YurService автоматически интегрирован в основное приложение. Все зависимости устанавливаются автоматически через npm workspaces, а общий код вынесен в `shared-src/`.

## 🚀 Быстрый старт

### Установка

```bash
npm install
```

Это автоматически установит зависимости для основного проекта и микросервиса.

### Запуск

```bash
npm run dev
```

Откройте `http://localhost:5173/yurservice` - страница будет работать с микросервисом.

### Сборка

```bash
npm run build
```

Микросервис автоматически включится в сборку основного приложения.

## ⚙️ Конфигурация

### Переменные окружения

Создайте `.env` файл в корне проекта:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

Микросервис автоматически использует эти переменные.

## 🏗️ Архитектура

### npm workspaces

Проект использует npm workspaces для управления зависимостями:
- Все зависимости устанавливаются в корневой `node_modules`
- Дублирование автоматически предотвращается
- Установка быстрее благодаря общей структуре

### Shared код (shared-src/)

Все общие компоненты, хуки и утилиты находятся в `shared-src/`:

```
shared-src/
├── components/
│   └── ui/              # UI компоненты (button, card, input, etc.)
├── hooks/
│   └── use-toast.ts     # Хук для toast уведомлений
├── lib/
│   ├── utils.ts         # Утилита cn() для классов
│   └── supabase.ts      # Supabase клиент
└── navigation/
    └── navigation-config.ts
```

Импорты используют алиас `@shared`:
```typescript
import { Button } from '@shared/components/ui/button'
import { useToast } from '@shared/hooks/use-toast'
import { supabase } from '@shared/lib/supabase'
```

### Интеграция

Микросервис импортируется напрямую в `src/App.tsx`:

```typescript
import { YurServicePage } from "../microfrontend-yurservice/src/components/yurservice/YurServicePage";
```

Vite автоматически обрабатывает импорты и разрешает алиасы через настроенный плагин.

## 📁 Структура проекта

```
ms-yurservice/
├── src/                          # Основное приложение
│   └── App.tsx                   # Импорт микросервиса
├── microfrontend-yurservice/     # Микросервис
│   └── src/
│       ├── components/
│       │   └── yurservice/      # Компоненты YurService
│       ├── hooks/               # Хуки микросервиса
│       └── lib/                 # Утилиты микросервиса
├── shared-src/                   # Общие компоненты, хуки и утилиты
│   ├── components/ui/           # UI компоненты
│   ├── hooks/                   # Общие хуки
│   └── lib/                     # Общие утилиты и Supabase
├── package.json                  # Корневой package.json с workspaces
└── vite.config.ts                # Плагин для алиасов
```

## 🔧 Оптимизация зависимостей

Все общие зависимости вынесены в `peerDependencies` микросервиса:
- React, ReactDOM и все @radix-ui пакеты
- Общие утилиты (clsx, tailwind-merge, lucide-react и т.д.)
- Supabase клиент

Это означает:
- ✅ Все зависимости устанавливаются только один раз в корневом проекте
- ✅ npm workspaces автоматически делает их доступными для микросервиса
- ✅ Нет дублирования пакетов
- ✅ Быстрее установка и меньше размер node_modules
- ✅ Гарантируется использование одинаковых версий

## 🎯 Преимущества shared-src

- **Нет дублирования** - один источник правды для общих компонентов
- **Легкое обновление** - изменили один раз, работает везде
- **Консистентность** - одинаковые компоненты во всем приложении
- **Простота импортов** - единый алиас `@shared`

## 🚢 Деплой

### Сборка

```bash
npm run build
```

Микросервис включается в общую сборку автоматически.

### Отдельный деплой микросервиса

Если нужно задеплоить микросервис отдельно:

```bash
cd microfrontend-yurservice
npm run build
```

Загрузите содержимое `dist/` на CDN или хостинг.

## 🐛 Поддержка

При проблемах проверьте:
1. Установлены ли зависимости: `npm install`
2. Настроены ли переменные окружения в `.env`
3. Работает ли основное приложение: `npm run dev`
4. Правильно ли настроены алиасы в `vite.config.ts` и `tsconfig.json`

## 📝 Следующие шаги

После успешной интеграции можно:
1. Вынести другие страницы в микросервисы
2. Настроить автоматический деплой
3. Добавить версионирование микросервисов
4. Расширить `shared-src/` новыми общими компонентами
