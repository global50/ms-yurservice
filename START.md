# Инструкция по запуску проекта

## 📦 Установка зависимостей

Зависимости устанавливаются автоматически через npm workspaces:

```bash
npm install
```

Это установит зависимости для:
- Основного приложения (`src/`)
- Микросервиса (`microfrontend-yurservice/`)
- Общих компонентов (`shared-src/`)

**Без дублирования** - все общие зависимости используются из корневого проекта.

## ⚙️ Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

Эти переменные используются как основным приложением, так и микросервисом.

## 🚀 Запуск

### Основное приложение с микросервисом

```bash
npm run dev
```

Откройте в браузере:
- `http://localhost:5173` - основное приложение
- `http://localhost:5173/yurservice` - страница микросервиса YurService

### Отдельный запуск микросервиса (опционально)

Если нужно разрабатывать микросервис отдельно:

```bash
cd microfrontend-yurservice
npm run dev
```

Микросервис будет доступен на `http://localhost:3001`

## 🏗️ Сборка

### Production сборка

```bash
npm run build
```

Сборка создаст папку `build/` с оптимизированным кодом. Микросервис автоматически включается в сборку.

### Предпросмотр production сборки

```bash
npm run build
npm run preview
```

## 📁 Структура проекта

```
ms-yurservice/
├── src/                          # Основное приложение
│   ├── components/              # Компоненты основного приложения
│   ├── lib/                     # Утилиты и API основного приложения
│   └── App.tsx                  # Главный компонент с роутингом
│
├── microfrontend-yurservice/     # Микросервис YurService
│   └── src/
│       ├── components/
│       │   └── yurservice/      # Компоненты микросервиса
│       ├── hooks/               # Хуки микросервиса
│       ├── lib/                 # Утилиты микросервиса
│       └── types/               # TypeScript типы
│
├── shared-src/                  # 🎯 Общие компоненты, хуки и утилиты
│   ├── components/
│   │   └── ui/                  # UI компоненты (button, card, input, etc.)
│   ├── hooks/
│   │   └── use-toast.ts         # Хук для toast уведомлений
│   ├── lib/
│   │   ├── utils.ts              # Утилита cn() для классов
│   │   └── supabase.ts           # Supabase клиент
│   └── navigation/
│       └── navigation-config.ts # Конфигурация навигации
│
└── package.json                  # Корневой package.json с workspaces
```

## 🔧 Что настроено

1. ✅ **npm workspaces** - автоматическая установка зависимостей
2. ✅ **Shared код** - все общее в `shared-src/` с алиасом `@shared`
3. ✅ **Микросервис интегрирован** - в `src/App.tsx`
4. ✅ **Vite конфигурация** - настроена для разрешения алиасов
5. ✅ **TypeScript конфигурация** - поддержка всех путей
6. ✅ **Оптимизация зависимостей** - без дублирования через peerDependencies
7. ✅ **Работа с БД** - через переменные окружения

## 🎯 Использование shared кода

Все общие компоненты, хуки и утилиты находятся в `shared-src/` и импортируются через алиас `@shared`:

```typescript
// UI компоненты
import { Button } from '@shared/components/ui/button'
import { Card } from '@shared/components/ui/card'
import { Input } from '@shared/components/ui/input'

// Хуки
import { useToast } from '@shared/hooks/use-toast'

// Утилиты
import { cn } from '@shared/lib/utils'
import { supabase } from '@shared/lib/supabase'
```

## ✅ Проверка работы

1. Установите зависимости: `npm install`
2. Настройте `.env` файл с переменными Supabase
3. Запустите: `npm run dev`
4. Откройте `http://localhost:5173/yurservice`
5. Должна открыться страница "Юр сервисы" с данными из БД

## 🐛 Решение проблем

### Ошибки при установке зависимостей

```bash
# Очистите node_modules и установите заново
rm -rf node_modules package-lock.json
npm install
```

### Ошибки импорта @shared

Убедитесь что:
- В `vite.config.ts` есть алиас `@shared`
- В `tsconfig.json` есть путь `@shared/*`
- Файлы существуют в `shared-src/`

### Ошибки подключения к БД

Проверьте:
- Файл `.env` существует в корне проекта
- Переменные `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY` заполнены
- Перезапустите dev сервер после изменения `.env`

## 📚 Дополнительная информация

- [README.md](./README.md) - общая информация о проекте
- [INTEGRATION_GUIDE.md](./INTEGRATION_GUIDE.md) - детали интеграции
- [REVIEW.md](./REVIEW.md) - отчет о выполненной работе
