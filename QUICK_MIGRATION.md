# Быстрая миграция YurService в отдельный репозиторий

## 🎯 Цель

Разделить микрофронтенд `microfrontend-yurservice` в отдельный репозиторий и подключить его как git submodule для независимого деплоя и разработки.

## ⚡ Быстрый старт (автоматически)

### Шаг 1: Убедитесь, что репозиторий создан

Репозиторий должен быть создан и пуст: `https://github.com/global50/remote-yurservice.git`

### Шаг 2: Запустите скрипт миграции

```bash
./scripts/migrate-yurservice-to-separate-repo.sh https://github.com/global50/remote-yurservice.git
```

Скрипт автоматически:
- ✅ Скопирует код в новый репозиторий
- ✅ Отправит код в GitHub
- ✅ Удалит из основного репозитория
- ✅ Добавит как git submodule

### Шаг 3: Закоммитьте изменения

```bash
git add .
git commit -m "Migrate yurservice to separate repository as submodule"
git push
```

## ✅ Проверка

После миграции проверьте:

1. **Репозиторий создан:**
   ```bash
   # Проверьте, что код есть в отдельном репозитории
   git ls-remote https://github.com/global50/remote-yurservice.git
   ```

2. **Submodule добавлен:**
   ```bash
   cat .gitmodules
   # Должна быть запись о microfrontend-yurservice
   ```

3. **Проект работает:**
   ```bash
   npm install
   npm run dev
   # Проверьте http://localhost:5173/yurservice
   ```

## 📚 Подробная документация

См. [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md) для детальной информации и решения проблем.

