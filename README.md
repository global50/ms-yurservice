# MS YurService

Основное приложение с интегрированным микросервисом YurService.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

Зависимости устанавливаются автоматически для основного проекта и микросервиса через npm workspaces (без дублирования).

### Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### Запуск в режиме разработки

```bash
npm run dev
```

Откройте `http://localhost:5173` - основное приложение  
Откройте `http://localhost:5173/yurservice` - страница микросервиса YurService

### Сборка для продакшена

```bash
npm run build
```

Микросервис автоматически включается в сборку основного приложения.

## 📁 Структура проекта

```
ms-yurservice/
├── src/                          # Основное приложение
│   ├── components/              # Компоненты основного приложения
│   ├── lib/                     # Утилиты и API
│   └── App.tsx                  # Главный компонент с роутингом
├── microfrontend-yurservice/     # Микросервис YurService
│   └── src/
│       ├── components/
│       │   └── yurservice/      # Компоненты микросервиса
│       ├── hooks/               # Хуки микросервиса
│       └── lib/                 # Утилиты микросервиса
├── shared-src/                  # Общие компоненты, хуки и утилиты
│   ├── components/
│   │   └── ui/                  # UI компоненты (button, card, input, etc.)
│   ├── hooks/
│   │   └── use-toast.ts         # Хук для toast уведомлений
│   ├── lib/
│   │   ├── utils.ts              # Утилита cn() для классов
│   │   └── supabase.ts           # Supabase клиент
│   └── navigation/
│       └── navigation-config.ts  # Конфигурация навигации
└── package.json                  # Корневой package.json с workspaces
```

## 🔧 Технические детали

### npm workspaces

Проект использует npm workspaces для управления зависимостями:
- Все зависимости устанавливаются в корневой `node_modules`
- Дублирование автоматически предотвращается
- Общие зависимости вынесены в `peerDependencies` микросервиса

### Shared код

Все общие компоненты, хуки и утилиты находятся в `shared-src/`:
- **UI компоненты**: `@shared/components/ui/*`
- **Хуки**: `@shared/hooks/*`
- **Утилиты**: `@shared/lib/*`
- **Supabase клиент**: `@shared/lib/supabase`

Импорты используют алиас `@shared`:
```typescript
import { Button } from '@shared/components/ui/button'
import { useToast } from '@shared/hooks/use-toast'
import { supabase } from '@shared/lib/supabase'
```

### Интеграция микросервиса

Микросервис импортируется напрямую в `src/App.tsx`:
```typescript
import { YurServicePage } from "../microfrontend-yurservice/src/components/yurservice/YurServicePage";
```

Vite автоматически обрабатывает импорты и разрешает алиасы через настроенные плагины.

## 📝 Скрипты

- `npm run dev` - запуск в режиме разработки
- `npm run build` - сборка для продакшена
- `npm run lint` - проверка кода линтером
- `npm run preview` - предпросмотр production сборки

## 🎯 Особенности

- ✅ **Без дублирования кода** - все общее в `shared-src/`
- ✅ **npm workspaces** - автоматическая установка зависимостей
- ✅ **Микросервисная архитектура** - изолированный код микросервиса
- ✅ **Общие зависимости** - используются из корня (peerDependencies)
- ✅ **TypeScript** - полная поддержка типов
- ✅ **Vite** - быстрая сборка и HMR

## 📚 Дополнительная документация

- [START.md](./START.md) - подробная инструкция по запуску
- [INTEGRATION_GUIDE.md](./INTEGRATION_GUIDE.md) - руководство по интеграции
- [REVIEW.md](./REVIEW.md) - отчет о выполненной работе
- [microfrontend-yurservice/README.md](./microfrontend-yurservice/README.md) - документация микросервиса
